---
description: 编码红线与推荐实践（合并 .cursor / .claude，通用 + TS）
alwaysApply: true
---

# 编码规范（速查）

**红线**必须遵守；**推荐**在可接受成本下尽量做到。

## 红线

### 不可变与数据

- 禁止 `push` / `splice` / `sort` / `reverse` 原地改数组；用展开、`filter`、`map`、`toSorted` / `toReversed` 等产生新数据
- 禁止把 `obj.x = …` 当常规更新；用 `{ …obj, key }` 或等价不可变写法
- 禁止对函数参数**重新赋值**（改参数引用）

### 错误、日志与 Promise

- 禁止空 `catch` 或仅注释的 `catch`；禁止无处理、无记录、不向上传递的「壳」`try/catch`
- 禁止忽略 Promise rejection（须 `await` 进 `try/catch` 或 `.catch` 等明确处理）
- 禁止 `console.log`；**失败与异常**须 `console.error`，并给用户可读提示或降级 UI，不暴露栈与敏感信息
- **非异常的弃用/降级**可用 `console.warn`，须克制、可行动

### 异步

- 主流程用 `async` / `await`；禁止用 `.then()` 链组织主要控制流（极薄兼容层除外）
- 禁止在循环里对**彼此独立**的请求逐个 `await`；应 `Promise.all`（或 `allSettled`）等并发；**有意串行**除外并加注释说明

### 魔法值、类型与命名

- 业务分支、权限、金额、状态、API 路径等禁止散落未命名数字/字符串；须命名常量或配置（枚举/字面量联合）
- 惯用字面量（如索引 `0`/`1`、空串）仅在不承载业务语义时可用
- TypeScript：**禁止 `any`**；用具体类型或 `unknown` + 收窄
- 禁止空洞命名：`data`、`info`、`temp`、`obj`、`arr`、`flag`、`result`；禁止自创缩写（公认除外：`id`、`url`、`api`、`dom`、`css`、`html`、`env`）
- 布尔以 `is` / `has` / `should` / `can` 开头（如 `isVisible`、`hasPermission`）

### 职责、规模与参数

- 单函数 **>50 行**或嵌套 **>4 层**须拆分；单文件 **>800 行**须拆模块
- 禁止「I/O + 复杂业务 + 展示格式化」混在同一函数且无分层（读数据 / 算结果 / 副作用分开）
- 禁止函数形参 **>3 个**；改为对象解构单参（`function fn({ a, b, c }: Props)`）
- 禁止上帝函数/文件；**同一抽象层级**内保持单一职责
- 禁止深层嵌套条件；用早返回（guard）摊平
- 同逻辑复制到**第三次**仍不抽取的粘贴式重复

### 安全

- 禁止 `eval`、`new Function()`；禁止未消毒的 `innerHTML` 拼接不可信内容
- 禁止硬编码密钥、token、密码、内部地址；走环境变量或密钥管理
- 日志与错误文案禁止输出令牌、密钥、完整证件号等；用户输入参与查询/命令/HTML 时须转义、参数化或白名单

## 推荐

- **类型**：对外边界（API、JSON）校验或收窄；优先收窄而非 `as`；对象形状可用 `interface`，联合/工具用 `type`；`as const` 收紧字面量
- **函数**：纯计算与 I/O 分离；参数较多时用对象解构；自定义 `Error` 子类带上下文；可恢复错误可用 Result/Optional，不可恢复再抛；在 API/UI 边界统一拦截
- **模块**：导出最小面；依赖注入优于硬编码依赖；弃用路径写明迁移
- **注释**：写「为什么」、非显然算法与安全/并发假设；公共 API 简短说明
- **依赖**：评估体积、维护、许可证；避免为小需求上大框架
- **测试**：核心纯函数与关键分支有自动化；修 bug 先补能复现的用例
- **性能**：独立请求并发、防抖/节流、大列表虚拟化、按需与惰性；React 场景注意不必要重渲染（`memo` / `useMemo` / `useCallback` 按需）
