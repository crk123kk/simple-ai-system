---
description: 通用样式红线（CSS / Less / Sass 等）
globs: "**/*.{css,scss,sass,less,styl,vue}"
alwaysApply: false
---

# 通用样式红线

适用于原生 CSS 与常见预处理器（Less、Sass/SCSS、Stylus 等）：约定**与具体预处理器语法无关**的通用约束；编写或修改样式时默认遵守。

## 命名与 BEM

- 类名 **kebab-case**。
- 组件样式用 **BEM**：`block`、`block__element`、`block--modifier`（Block 用 `-` 连接单词，Element 用 `__`，Modifier 用 `--`）。
- **Element 只一层**：禁止 `block__a__b__c` 式堆叠。子结构用新 Block 或扁平类名表达，不镜像完整 DOM 路径。
- Modifier 表语义（如 `--primary`），避免纯视觉词（如 `--red`），除非设计令牌即如此命名。
- 样式只用 **class**。禁止 `#id` 做样式钩子。禁止 `tag.class` 提高权重（特例须注释）。
- **JS 钩子** 使用 `js-` 前缀类名，与样式类分离。
- 工具类集中、可检索命名（如 `u-` 前缀），避免与 BEM 块名冲突。
- 避免全局「标签后代」大网（如任意 `div span`）耦合业务结构。

## 嵌套、链式与权重

- 预处理器嵌套 **≤3 层**（含根；伪类/伪元素计一层）。更深则拆类名或拆文件。
- 能一条扁平选择器写清则不强行嵌套。
- 禁止过长后代链（`.a .b .c .d`）；目标节点优先有独立 class。
- 保持 **低特异性**，不靠堆叠标签或父级链「压过」旧样式。
- 同一规则集内避免同一属性重复声明、靠后者顺序掩盖意图。
- **`!important`**：默认不用。仅工具类、或覆盖不可改第三方样式时使用，且须注释原因。禁止用它修补可重构的权重问题。

## 设计令牌与数值

- 颜色、间距、圆角、阴影、字体阶梯等须走 **CSS 变量或预处理变量**（设计令牌），禁止散落魔法数。
- **z-index** 用档位或 token，禁止随意极大值堆叠。
- `0` 省略单位。
- 颜色与单位写法与项目约定一致，避免同义多写法混用。

## 布局与响应式

- 布局优先 **Flex / Grid**。能用 `gap` 则少用负 margin 技巧；慎用脱离文档流的「硬凑」。
- **移动优先**：默认窄屏样式，用 `min-width` 向上增强。
- 断点值走变量，禁止魔法断点。媒体查询与相关规则就近书写。

## 预处理器与抽象

- 变量须 **语义化**（如主题/间距名），按类型分组。
- Mixin **单一职责**；禁止用 mixin 吐大段无关样式。
- **禁止 `@extend` / `:extend()`**（可追踪性差、易合并意外规则）；用多类组合或共用 mixin/变量替代。
- 只抽象有设计语义的重复；碰巧相同的一条声明不必抽象。**重复优于错误抽象**。

## 声明顺序与格式

- 属性顺序建议分组（组间可空行）：
  - 定位（含 `z-index`）
  - 布局（display / flex / grid / overflow 等）
  - 盒模型
  - 排版
  - 视觉
  - 动效
- 格式跟随项目格式化配置。无约定时：
  - 2 空格缩进
  - `{` 前空格、`:` 后空格
  - 每条声明一行且分号结尾
  - 相关选择器可并列换行

## 可访问性与交互

- 可见焦点：禁止去掉 `outline` 而不提供等价可见焦点。定制用 **`:focus-visible`**。
- 尊重 **`prefers-reduced-motion`**：非必要动效在 reduce 下关闭或减弱。
- 对比度与字号满足无障碍要求。
- 信息不单独依赖颜色时，须有文案或图标辅助。

## Vue 单文件组件

- `scoped` 中 **节制** `:deep()` / `::v-deep`，只穿透必要节点。
- 优先子组件根 class 或 **CSS 变量** 协商边界，避免深嵌套「命中」子树。

## 文件与职责

- 大文件分段：变量/令牌 → 布局 → 块 → 工具类；模块间用简短分隔注释。
- 删除长期保留的大段注释死代码。
- 单一职责、**组合优于巨型类**。
- 布局与皮肤（主题色等）尽量可拆分组合。
- 禁止依赖内联 `style=""`（除非框架生成的动态值且无法用类表达）。
- 新布局避免以 `float` 为主排版；遗留 float 仅作兼容时局部使用。

## 禁止清单（速查）

- ID 选择器作样式
- `tag.class` 提权（无注释特例）
- 驼峰 / 下划线类名
- BEM Element 多层堆叠
- 嵌套超过 3 层
- 过长后代链
- 滥用 `!important`
- `@extend` / `:extend()`
- 魔法数、魔法断点
- 内联样式滥用
- 随意极大 z-index
- 全局标签选择器网
- 无注释的第三方覆盖
